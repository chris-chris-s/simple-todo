describe("TodoApp.js", function () {
  let todoApp;
  beforeEach(() => {
    todoApp = new TodoApp();
  });

  it("should add todo to empty list", () => {});

  it("should remove todo from list", () => {});

  it("should mark todo as complete", () => {});

  it("should add important tasks", () => {});

  it("should show done tasks", () => {});
});
